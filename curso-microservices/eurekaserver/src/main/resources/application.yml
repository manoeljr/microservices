spring:
  application:
    name: eurekaserver
  security:
    basic:
      enabled: true
    user:
      name: cursoma-eureka-user
      password: ecok-usr-eeuramssur

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false # N�o registrar o eureka server
    fetch-registry: false
